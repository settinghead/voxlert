# Changesets

Add a changeset for any change that affects users of the published CLI.

Typical examples:

- new commands or flags
- behavior changes
- packaging or install changes
- integration changes
- docs that change how users set up or operate the CLI

Run `npm run changeset` and commit the generated markdown file.
